import * as actionTypes from "../actions/types";
const initialState = {
  items: []
};

const reducer = (state = initialState, type) => {
  switch (type) {
    default:
      return state;
  }
};
export default reducer;
